# Dynamic virtual host snippets go here.
# Example: define server/location blocks that proxy selected hosts to protected apps.
# This placeholder keeps /etc/nginx/conf.d/*.conf include stable when no targets are configured yet.
